nelmio_api_doc:
    documentation:
        info:
            title: ReSell Messaging Service API
            description: Messaging microservice for conversations between buyers and sellers
            version: 1.0.0
        components:
            securitySchemes:
                Bearer:
                    type: http
                    scheme: bearer
                    bearerFormat: JWT
        security:
            - Bearer: []
    areas:
        path_patterns:
            - ^/api(?!/doc)

