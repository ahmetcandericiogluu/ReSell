=================================================================
🚀 RESELL - DEPLOYMENT INSTRUCTIONS
=================================================================

📁 Hazırlanan Dosyalar:
✅ Dockerfile - Production ready
✅ compose.yaml - Docker Compose config
✅ build.sh - Build script
✅ docker-entrypoint.sh - Container startup script
✅ Procfile - Heroku/Railway için
✅ .dockerignore - Docker build optimize
✅ render.yaml - Render.com config
✅ DEPLOYMENT.md - Detaylı deployment rehberi

=================================================================
🎯 HIZLI DEPLOYMENT (3 Seçenek)
=================================================================

SEÇENEK 1: RENDER.COM (ÖNERİLEN - EN KOLAY)
--------------------------------------------
1. Git repo oluştur:
   git init
   git add .
   git commit -m "Initial commit"
   git remote add origin <your-github-repo>
   git push -u origin main

2. https://render.com → Sign up / Login

3. Dashboard → "New +" → "Web Service"
   - Connect GitHub repo
   - Runtime: Docker
   - Plan: Free

4. Environment Variables ekle:
   APP_ENV=prod
   APP_SECRET=<random-32-char-string>
   
5. PostgreSQL ekle:
   Dashboard → "New +" → "PostgreSQL"
   - Name: resell-db
   - Plan: Free
   
6. Web Service'e DATABASE_URL ekle:
   - PostgreSQL'den "Internal Connection String" kopyala
   - Web Service → Environment → Add
   - Key: DATABASE_URL
   - Value: <connection-string>

7. "Deploy" butonuna tıkla!

✅ 5-10 dakikada canlıda!


SEÇENEK 2: RAILWAY.APP (HIZLI)
-------------------------------
1. Git push (yukarıdaki gibi)

2. https://railway.app → Login with GitHub

3. "New Project" → "Deploy from GitHub repo"

4. "New" → "Database" → "Add PostgreSQL"

5. Environment Variables:
   APP_ENV=prod
   APP_SECRET=<random-string>
   DATABASE_URL=${{Postgres.DATABASE_URL}}

6. Settings:
   Build Command: bash build.sh
   Start Command: php -S 0.0.0.0:$PORT -t public

✅ Otomatik deploy!


SEÇENEK 3: LOKAL DOCKER (TEST)
-------------------------------
1. Docker Desktop çalıştır

2. Komutlar:
   docker-compose up -d
   docker-compose exec web php bin/console doctrine:migrations:migrate --no-interaction

3. Tarayıcıda aç:
   http://localhost:8000

✅ Lokal olarak çalışıyor!

=================================================================
📋 DEPLOYMENT CHECKLIST
=================================================================
☐ Git repo oluşturuldu ve push edildi
☐ Deployment platform seçildi (Render/Railway/Heroku)
☐ PostgreSQL database eklendi
☐ Environment variables ayarlandı:
   - APP_ENV=prod
   - APP_SECRET (güçlü random string)
   - DATABASE_URL (PostgreSQL connection string)
☐ Deploy edildi ve build başarılı
☐ Migration'lar otomatik çalıştı
☐ Test edildi (register, login endpoints)

=================================================================
🧪 PRODUCTION TEST
=================================================================
Deploy sonrası test et:

# Ana sayfa
curl https://your-app.onrender.com/

# Register
curl -X POST https://your-app.onrender.com/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{"email":"test@example.com","password":"123456","name":"Test User"}'

# Login
curl -X POST https://your-app.onrender.com/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email":"test@example.com","password":"123456"}'

=================================================================
📚 DOKÜMANTASYON
=================================================================
Detaylı deployment rehberi için:
- DEPLOYMENT.md dosyasını oku
- README_DEPLOY.md dosyasını oku

Sorular için:
- DEPLOYMENT.md → Sorun Giderme bölümü

=================================================================
🎉 BAŞARILAR!
=================================================================

