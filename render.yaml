# Render Blueprint for ReSell Marketplace
# https://render.com/docs/blueprint-spec

databases:
  - name: auth-db
    databaseName: auth_service
    user: auth_user
    plan: free
    region: frankfurt
    ipAllowList: []

  - name: listing-db
    databaseName: listing_service
    user: listing_user
    plan: free
    region: frankfurt
    ipAllowList: []

  - name: backend-db
    databaseName: resell_db
    user: resell_user
    plan: free
    region: frankfurt
    ipAllowList: []

  # messaging-db is hosted on Neon (external) - Render free plan limit

services:
  # Auth Service
  - type: web
    name: resell-auth-service
    runtime: docker
    region: frankfurt
    plan: free
    dockerfilePath: ./auth-service/Dockerfile.render
    dockerContext: ./auth-service
    envVars:
      - key: APP_ENV
        value: prod
      - key: APP_DEBUG
        value: 0
      - key: APP_SECRET
        generateValue: true
      - key: DATABASE_URL
        fromDatabase:
          name: auth-db
          property: connectionString
      - key: CORS_ALLOW_ORIGIN
        value: ^https?://(localhost|127\.0\.0\.1|.*\.onrender\.com)(:\d+)?$
    healthCheckPath: /health

  # Listing Service
  - type: web
    name: resell-listing-service
    runtime: docker
    region: frankfurt
    plan: free
    dockerfilePath: ./services/listing/Dockerfile.render
    dockerContext: ./services/listing
    envVars:
      - key: APP_ENV
        value: prod
      - key: APP_DEBUG
        value: 0
      - key: APP_SECRET
        generateValue: true
      - key: DATABASE_URL
        fromDatabase:
          name: listing-db
          property: connectionString
      - key: CORS_ALLOW_ORIGIN
        value: ^https?://(localhost|127\.0\.0\.1|.*\.onrender\.com)(:\d+)?$
      - key: DEFAULT_URI
        value: https://resell-listing-service.onrender.com
      # Elasticsearch - Set manually in Render Dashboard
      - key: ELASTICSEARCH_URL
        sync: false
      - key: ELASTICSEARCH_API_KEY
        sync: false
      - key: FORCE_REINDEX
        sync: false
    healthCheckPath: /health

  # Backend Monolith
  - type: web
    name: resell-backend
    runtime: docker
    region: frankfurt
    plan: free
    dockerfilePath: ./backend/Dockerfile.render
    dockerContext: ./backend
    envVars:
      - key: APP_ENV
        value: prod
      - key: APP_DEBUG
        value: 0
      - key: APP_SECRET
        generateValue: true
      - key: DATABASE_URL
        fromDatabase:
          name: backend-db
          property: connectionString
      - key: CORS_ALLOW_ORIGIN
        value: ^https?://(localhost|127\.0\.0\.1|.*\.onrender\.com)(:\d+)?$
      - key: FRONTEND_URL
        value: https://resell-frontend.onrender.com
    healthCheckPath: /

  # Messaging Service (uses Neon PostgreSQL - external DB)
  - type: web
    name: resell-messaging-service
    runtime: docker
    region: frankfurt
    plan: free
    dockerfilePath: ./services/messaging/Dockerfile.render
    dockerContext: ./services/messaging
    envVars:
      - key: APP_ENV
        value: prod
      - key: APP_DEBUG
        value: 0
      - key: APP_SECRET
        generateValue: true
      # DATABASE_URL from Neon - set manually in Render Dashboard
      # Format: postgresql://USER:PASS@ep-xxx.neon.tech/messaging_service?sslmode=require
      - key: DATABASE_URL
        sync: false
      - key: CORS_ALLOW_ORIGIN
        value: ^https?://(localhost|127\.0\.0\.1|.*\.onrender\.com)(:\d+)?$
      - key: AUTH_SERVICE_URL
        value: https://resell-auth-service.onrender.com
      - key: LISTING_SERVICE_URL
        value: https://resell-listing-service.onrender.com
    healthCheckPath: /health

  # Frontend
  - type: web
    name: resell-frontend
    runtime: node
    region: frankfurt
    plan: free
    buildCommand: cd frontend && npm ci && npm run build
    startCommand: cd frontend && npm run preview -- --host 0.0.0.0 --port $PORT
    envVars:
      - key: VITE_API_URL
        value: https://resell-backend.onrender.com
      - key: VITE_AUTH_SERVICE_URL
        value: https://resell-auth-service.onrender.com
      - key: VITE_LISTING_SERVICE_URL
        value: https://resell-listing-service.onrender.com
      - key: VITE_MESSAGING_SERVICE_URL
        value: https://resell-messaging-service.onrender.com
    healthCheckPath: /
