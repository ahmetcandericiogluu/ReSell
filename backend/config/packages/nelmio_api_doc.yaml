nelmio_api_doc:
    documentation:
        info:
            title: ReSell Backend API
            description: Main API for ReSell platform - users, listings, reviews
            version: 1.0.0
        servers:
            - url: /
              description: Current Server
        components:
            securitySchemes:
                Bearer:
                    type: http
                    scheme: bearer
                    bearerFormat: JWT
        security:
            - Bearer: []
    areas:
        path_patterns:
            - ^/api(?!/doc)

